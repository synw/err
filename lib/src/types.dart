/// The error channels
enum ErrChannel {
  /// The developpement channel
  dev,

  /// The production channel
  production
}

/// The error channels
enum ErrType {
  /// Critical errors
  critical,

  /// Normal errors
  error,

  /// Warning level
  warning,

  /// Info level
  info,

  /// Debug level
  debug
}
